public final class androidx.browser.trusted.Token extends java.lang.Object
{
    private static final java.lang.String TAG;
    private final androidx.browser.trusted.TokenContents mContents;

    private void <init>(androidx.browser.trusted.TokenContents)
    {
        androidx.browser.trusted.TokenContents $r1;
        androidx.browser.trusted.Token r0;

        r0 := @this;

        $r1 := @parameter0;

        specialinvoke r0.<init>();

        r0.mContents = $r1;

        return;
    }

    public static androidx.browser.trusted.Token create(java.lang.String, android.content.pm.PackageManager)
    {
        java.util.List $r2;
        androidx.browser.trusted.Token r3;
        android.content.pm.PackageManager $r1;
        androidx.browser.trusted.TokenContents $r4;
        java.io.IOException r5;
        java.lang.String $r0;

        $r0 := @parameter0;

        $r1 := @parameter1;

        $r2 = androidx.browser.trusted.PackageIdentityUtils.getFingerprintsForPackage($r0, $r1);

        if $r2 != null goto label1;

        return null;

     label1:
        r3 = new androidx.browser.trusted.Token;

     label2:
        $r4 = androidx.browser.trusted.TokenContents.create($r0, $r2);

        specialinvoke r3.<init>($r4);

     label3:
        return r3;

     label4:
        r5 := @caughtexception;

        android.util.Log.e("Token", "Exception when creating token.", r5);

        return null;

        catch java.io.IOException from label2 to label3 with label4;
    }

    public static androidx.browser.trusted.Token deserialize(byte[])
    {
        androidx.browser.trusted.TokenContents $r2;
        byte[] $r0;
        androidx.browser.trusted.Token $r1;

        $r0 := @parameter0;

        $r1 = new androidx.browser.trusted.Token;

        $r2 = androidx.browser.trusted.TokenContents.deserialize($r0);

        specialinvoke $r1.<init>($r2);

        return $r1;
    }

    public boolean matches(java.lang.String, android.content.pm.PackageManager)
    {
        androidx.browser.trusted.Token r0;
        android.content.pm.PackageManager $r2;
        androidx.browser.trusted.TokenContents $r3;
        java.lang.String $r1;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

        $r2 := @parameter1;

        $r3 = r0.mContents;

        $z0 = androidx.browser.trusted.PackageIdentityUtils.packageMatchesToken($r1, $r2, $r3);

        return $z0;
    }

    public byte[] serialize()
    {
        androidx.browser.trusted.TokenContents $r2;
        byte[] $r1;
        androidx.browser.trusted.Token r0;

        r0 := @this;

        $r2 = r0.mContents;

        $r1 = virtualinvoke $r2.serialize();

        return $r1;
    }
}
