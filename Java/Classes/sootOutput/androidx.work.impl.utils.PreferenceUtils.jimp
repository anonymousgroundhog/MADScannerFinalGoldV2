public class androidx.work.impl.utils.PreferenceUtils extends java.lang.Object
{
    public static final java.lang.String KEY_LAST_CANCEL_ALL_TIME_MS;
    public static final java.lang.String KEY_RESCHEDULE_NEEDED;
    public static final java.lang.String PREFERENCES_FILE_NAME;
    private final androidx.work.impl.WorkDatabase mWorkDatabase;

    public void <init>(androidx.work.impl.WorkDatabase)
    {
        androidx.work.impl.utils.PreferenceUtils r0;
        androidx.work.impl.WorkDatabase $r1;

        r0 := @this;

        $r1 := @parameter0;

        specialinvoke r0.<init>();

        r0.mWorkDatabase = $r1;

        return;
    }

    public static void migrateLegacyPreferences(android.content.Context, androidx.sqlite.db.SupportSQLiteDatabase)
    {
        android.content.SharedPreferences$Editor $r5;
        java.lang.Throwable $r6;
        android.content.Context $r0;
        java.lang.Object[] $r2;
        long l0, $l1;
        androidx.sqlite.db.SupportSQLiteDatabase $r1;
        android.content.SharedPreferences $r3;
        java.lang.Long $r4;
        boolean $z0;

        $r0 := @parameter0;

        $r1 := @parameter1;

        $r3 = virtualinvoke $r0.getSharedPreferences("androidx.work.util.preferences", 0);

        $z0 = interfaceinvoke $r3.contains("reschedule_needed");

        if $z0 != 0 goto label1;

        $z0 = interfaceinvoke $r3.contains("last_cancel_all_time_ms");

        if $z0 == 0 goto label6;

     label1:
        l0 = 0L;

        $l1 = interfaceinvoke $r3.getLong("last_cancel_all_time_ms", 0L);

        $z0 = interfaceinvoke $r3.getBoolean("reschedule_needed", 0);

        if $z0 == 0 goto label2;

        l0 = 1L;

     label2:
        interfaceinvoke $r1.beginTransaction();

     label3:
        $r2 = newarray (java.lang.Object)[2];

        $r2[0] = "last_cancel_all_time_ms";

        $r4 = java.lang.Long.valueOf($l1);

        $r2[1] = $r4;

        interfaceinvoke $r1.execSQL("INSERT OR REPLACE INTO `Preference` (`key`, `long_value`) VALUES (@key, @long_value)", $r2);

        $r2 = newarray (java.lang.Object)[2];

        $r2[0] = "reschedule_needed";

        $r4 = java.lang.Long.valueOf(l0);

        $r2[1] = $r4;

        interfaceinvoke $r1.execSQL("INSERT OR REPLACE INTO `Preference` (`key`, `long_value`) VALUES (@key, @long_value)", $r2);

        $r5 = interfaceinvoke $r3.edit();

        $r5 = interfaceinvoke $r5.clear();

        interfaceinvoke $r5.apply();

        interfaceinvoke $r1.setTransactionSuccessful();

     label4:
        interfaceinvoke $r1.endTransaction();

        return;

     label5:
        $r6 := @caughtexception;

        interfaceinvoke $r1.endTransaction();

        throw $r6;

     label6:
        return;

        catch java.lang.Throwable from label3 to label4 with label5;
    }

    public long getLastCancelAllTimeMillis()
    {
        java.lang.Long $r3;
        androidx.work.impl.WorkDatabase r1;
        androidx.work.impl.utils.PreferenceUtils r0;
        long $l0;
        androidx.work.impl.model.PreferenceDao $r2;

        r0 := @this;

        r1 = r0.mWorkDatabase;

        $r2 = virtualinvoke r1.preferenceDao();

        $r3 = interfaceinvoke $r2.getLongValue("last_cancel_all_time_ms");

        if $r3 == null goto label1;

        $l0 = virtualinvoke $r3.longValue();

        return $l0;

     label1:
        return 0L;
    }

    public androidx.lifecycle.LiveData getLastCancelAllTimeMillisLiveData()
    {
        androidx.lifecycle.LiveData $r3;
        androidx.work.impl.WorkDatabase r1;
        androidx.work.impl.utils.PreferenceUtils$1 $r4;
        androidx.work.impl.utils.PreferenceUtils r0;
        androidx.work.impl.model.PreferenceDao $r2;

        r0 := @this;

        r1 = r0.mWorkDatabase;

        $r2 = virtualinvoke r1.preferenceDao();

        $r3 = interfaceinvoke $r2.getObservableLongValue("last_cancel_all_time_ms");

        $r4 = new androidx.work.impl.utils.PreferenceUtils$1;

        specialinvoke $r4.<init>(r0);

        $r3 = androidx.lifecycle.Transformations.map($r3, $r4);

        return $r3;
    }

    public boolean getNeedsReschedule()
    {
        byte $b0;
        java.lang.Long $r3;
        androidx.work.impl.WorkDatabase r1;
        androidx.work.impl.utils.PreferenceUtils r0;
        long $l1;
        androidx.work.impl.model.PreferenceDao $r2;

        r0 := @this;

        r1 = r0.mWorkDatabase;

        $r2 = virtualinvoke r1.preferenceDao();

        $r3 = interfaceinvoke $r2.getLongValue("reschedule_needed");

        if $r3 == null goto label1;

        $l1 = virtualinvoke $r3.longValue();

        $b0 = $l1 cmp 1L;

        if $b0 != 0 goto label1;

        return 1;

     label1:
        return 0;
    }

    public void setLastCancelAllTimeMillis(long)
    {
        androidx.work.impl.model.Preference r1;
        androidx.work.impl.WorkDatabase $r3;
        androidx.work.impl.utils.PreferenceUtils r0;
        long $l0;
        androidx.work.impl.model.PreferenceDao $r2;

        r0 := @this;

        $l0 := @parameter0;

        r1 = new androidx.work.impl.model.Preference;

        specialinvoke r1.<init>("last_cancel_all_time_ms", $l0);

        $r3 = r0.mWorkDatabase;

        $r2 = virtualinvoke $r3.preferenceDao();

        interfaceinvoke $r2.insertPreference(r1);

        return;
    }

    public void setNeedsReschedule(boolean)
    {
        androidx.work.impl.model.Preference r1;
        androidx.work.impl.WorkDatabase $r3;
        androidx.work.impl.utils.PreferenceUtils r0;
        androidx.work.impl.model.PreferenceDao $r2;
        boolean $z0;

        r0 := @this;

        $z0 := @parameter0;

        r1 = new androidx.work.impl.model.Preference;

        specialinvoke r1.<init>("reschedule_needed", $z0);

        $r3 = r0.mWorkDatabase;

        $r2 = virtualinvoke $r3.preferenceDao();

        interfaceinvoke $r2.insertPreference(r1);

        return;
    }
}
