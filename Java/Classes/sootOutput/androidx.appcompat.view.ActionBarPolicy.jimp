public class androidx.appcompat.view.ActionBarPolicy extends java.lang.Object
{
    private android.content.Context mContext;

    private void <init>(android.content.Context)
    {
        android.content.Context $r1;
        androidx.appcompat.view.ActionBarPolicy r0;

        r0 := @this;

        $r1 := @parameter0;

        specialinvoke r0.<init>();

        r0.mContext = $r1;

        return;
    }

    public static androidx.appcompat.view.ActionBarPolicy get(android.content.Context)
    {
        android.content.Context $r0;
        androidx.appcompat.view.ActionBarPolicy $r1;

        $r0 := @parameter0;

        $r1 = new androidx.appcompat.view.ActionBarPolicy;

        specialinvoke $r1.<init>($r0);

        return $r1;
    }

    public boolean enableHomeButtonByDefault()
    {
        android.content.Context $r2;
        int $i0;
        android.content.pm.ApplicationInfo $r1;
        androidx.appcompat.view.ActionBarPolicy r0;

        r0 := @this;

        $r2 = r0.mContext;

        $r1 = virtualinvoke $r2.getApplicationInfo();

        $i0 = $r1.targetSdkVersion;

        if $i0 >= 14 goto label1;

        return 1;

     label1:
        return 0;
    }

    public int getEmbeddedMenuWidthLimit()
    {
        android.content.Context $r1;
        int $i0;
        androidx.appcompat.view.ActionBarPolicy r0;
        android.util.DisplayMetrics $r3;
        android.content.res.Resources $r2;

        r0 := @this;

        $r1 = r0.mContext;

        $r2 = virtualinvoke $r1.getResources();

        $r3 = virtualinvoke $r2.getDisplayMetrics();

        $i0 = $r3.widthPixels;

        $i0 = $i0 / 2;

        return $i0;
    }

    public int getMaxActionButtons()
    {
        android.content.Context r1;
        android.content.res.Configuration $r3;
        int i0, i1, i2;
        androidx.appcompat.view.ActionBarPolicy r0;
        android.content.res.Resources $r2;

        r0 := @this;

        r1 = r0.mContext;

        $r2 = virtualinvoke r1.getResources();

        $r3 = virtualinvoke $r2.getConfiguration();

        i0 = $r3.screenWidthDp;

        i1 = $r3.screenHeightDp;

        i2 = $r3.smallestScreenWidthDp;

        if i2 > 600 goto label7;

        if i0 > 600 goto label7;

        if i0 <= 960 goto label1;

        if i1 > 720 goto label7;

     label1:
        if i0 <= 720 goto label2;

        if i1 <= 960 goto label2;

        goto label7;

     label2:
        if i0 >= 500 goto label6;

        if i0 <= 640 goto label3;

        if i1 > 480 goto label6;

     label3:
        if i0 <= 480 goto label4;

        if i1 <= 640 goto label4;

        goto label6;

     label4:
        if i0 < 360 goto label5;

        return 3;

     label5:
        return 2;

     label6:
        return 4;

     label7:
        return 5;
    }

    public int getStackedTabMaxWidth()
    {
        android.content.Context $r1;
        int $i0;
        androidx.appcompat.view.ActionBarPolicy r0;
        android.content.res.Resources $r2;

        r0 := @this;

        $r1 = r0.mContext;

        $r2 = virtualinvoke $r1.getResources();

        $i0 = androidx.appcompat.R$dimen.abc_action_bar_stacked_tab_max_width;

        $i0 = virtualinvoke $r2.getDimensionPixelSize($i0);

        return $i0;
    }

    public int getTabContainerHeight()
    {
        android.content.Context r1;
        android.content.res.TypedArray $r3;
        int[] r2;
        int i0, i1;
        androidx.appcompat.view.ActionBarPolicy r0;
        android.content.res.Resources $r4;
        boolean $z0;

        r0 := @this;

        r1 = r0.mContext;

        r2 = androidx.appcompat.R$styleable.ActionBar;

        i0 = androidx.appcompat.R$attr.actionBarStyle;

        $r3 = virtualinvoke r1.obtainStyledAttributes(null, r2, i0, 0);

        i0 = androidx.appcompat.R$styleable.ActionBar_height;

        i0 = virtualinvoke $r3.getLayoutDimension(i0, 0);

        i1 = i0;

        r1 = r0.mContext;

        $r4 = virtualinvoke r1.getResources();

        $z0 = virtualinvoke r0.hasEmbeddedTabs();

        if $z0 != 0 goto label1;

        i1 = androidx.appcompat.R$dimen.abc_action_bar_stacked_max_height;

        i1 = virtualinvoke $r4.getDimensionPixelSize(i1);

        i1 = java.lang.Math.min(i0, i1);

     label1:
        virtualinvoke $r3.recycle();

        return i1;
    }

    public boolean hasEmbeddedTabs()
    {
        android.content.Context $r1;
        int $i0;
        androidx.appcompat.view.ActionBarPolicy r0;
        android.content.res.Resources $r2;
        boolean $z0;

        r0 := @this;

        $r1 = r0.mContext;

        $r2 = virtualinvoke $r1.getResources();

        $i0 = androidx.appcompat.R$bool.abc_action_bar_embed_tabs;

        $z0 = virtualinvoke $r2.getBoolean($i0);

        return $z0;
    }

    public boolean showsOverflowMenuButton()
    {
        androidx.appcompat.view.ActionBarPolicy r0;

        r0 := @this;

        return 1;
    }
}
