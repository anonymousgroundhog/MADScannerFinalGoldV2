public class androidx.core.widget.ListViewAutoScrollHelper extends androidx.core.widget.AutoScrollHelper
{
    private final android.widget.ListView mTarget;

    public void <init>(android.widget.ListView)
    {
        android.widget.ListView $r1;
        androidx.core.widget.ListViewAutoScrollHelper r0;

        r0 := @this;

        $r1 := @parameter0;

        specialinvoke r0.<init>($r1);

        r0.mTarget = $r1;

        return;
    }

    public boolean canTargetScrollHorizontally(int)
    {
        int $i0;
        androidx.core.widget.ListViewAutoScrollHelper r0;

        r0 := @this;

        $i0 := @parameter0;

        return 0;
    }

    public boolean canTargetScrollVertically(int)
    {
        int $i0, i1, $i2, $i3, $i4;
        android.view.View $r2;
        android.widget.ListView r1;
        androidx.core.widget.ListViewAutoScrollHelper r0;

        r0 := @this;

        $i0 := @parameter0;

        r1 = r0.mTarget;

        $i2 = virtualinvoke r1.getCount();

        if $i2 != 0 goto label1;

        return 0;

     label1:
        $i3 = virtualinvoke r1.getChildCount();

        $i4 = virtualinvoke r1.getFirstVisiblePosition();

        i1 = $i4 + $i3;

        if $i0 <= 0 goto label3;

        if i1 < $i2 goto label4;

        $i0 = $i3 + -1;

        $r2 = virtualinvoke r1.getChildAt($i0);

        $i0 = virtualinvoke $r2.getBottom();

        $i3 = virtualinvoke r1.getHeight();

        if $i0 > $i3 goto label2;

        return 0;

     label2:
        goto label4;

     label3:
        if $i0 >= 0 goto label5;

        if $i4 > 0 goto label4;

        $r2 = virtualinvoke r1.getChildAt(0);

        $i0 = virtualinvoke $r2.getTop();

        if $i0 < 0 goto label4;

        return 0;

     label4:
        return 1;

     label5:
        return 0;
    }

    public void scrollTargetBy(int, int)
    {
        int $i0, $i1;
        android.widget.ListView $r1;
        androidx.core.widget.ListViewAutoScrollHelper r0;

        r0 := @this;

        $i0 := @parameter0;

        $i1 := @parameter1;

        $r1 = r0.mTarget;

        androidx.core.widget.ListViewCompat.scrollListBy($r1, $i1);

        return;
    }
}
