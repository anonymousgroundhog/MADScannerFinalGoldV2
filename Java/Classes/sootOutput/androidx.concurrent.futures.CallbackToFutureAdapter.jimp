public final class androidx.concurrent.futures.CallbackToFutureAdapter extends java.lang.Object
{

    private void <init>()
    {
        androidx.concurrent.futures.CallbackToFutureAdapter r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static com.google.common.util.concurrent.ListenableFuture getFuture(androidx.concurrent.futures.CallbackToFutureAdapter$Resolver)
    {
        androidx.concurrent.futures.CallbackToFutureAdapter$Completer r1;
        androidx.concurrent.futures.CallbackToFutureAdapter$SafeFuture r2;
        androidx.concurrent.futures.CallbackToFutureAdapter$Resolver r0;
        java.lang.Class $r3;
        java.lang.Object $r4;
        java.lang.Exception r5;

        r0 := @parameter0;

        r1 = new androidx.concurrent.futures.CallbackToFutureAdapter$Completer;

        specialinvoke r1.<init>();

        r2 = new androidx.concurrent.futures.CallbackToFutureAdapter$SafeFuture;

        specialinvoke r2.<init>(r1);

        r1.future = r2;

        $r3 = virtualinvoke r0.getClass();

        r1.tag = $r3;

     label1:
        $r4 = interfaceinvoke r0.attachCompleter(r1);

     label2:
        if $r4 == null goto label4;

     label3:
        r1.tag = $r4;

     label4:
        return r2;

     label5:
        r5 := @caughtexception;

        virtualinvoke r2.setException(r5);

        return r2;

        catch java.lang.Exception from label1 to label2 with label5;
        catch java.lang.Exception from label3 to label4 with label5;
    }
}
