public class androidx.core.app.AppLaunchChecker extends java.lang.Object
{
    private static final java.lang.String KEY_STARTED_FROM_LAUNCHER;
    private static final java.lang.String SHARED_PREFS_NAME;

    public void <init>()
    {
        androidx.core.app.AppLaunchChecker r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static boolean hasStartedFromLauncher(android.content.Context)
    {
        android.content.Context $r0;
        android.content.SharedPreferences $r1;
        boolean $z0;

        $r0 := @parameter0;

        $r1 = virtualinvoke $r0.getSharedPreferences("android.support.AppLaunchChecker", 0);

        $z0 = interfaceinvoke $r1.getBoolean("startedFromLauncher", 0);

        return $z0;
    }

    public static void onActivityCreate(android.app.Activity)
    {
        android.content.SharedPreferences$Editor $r5;
        android.content.SharedPreferences $r1;
        android.content.Intent $r2;
        java.lang.String $r3, $r4;
        android.app.Activity $r0;
        boolean $z0;

        $r0 := @parameter0;

        $r1 = virtualinvoke $r0.getSharedPreferences("android.support.AppLaunchChecker", 0);

        $z0 = interfaceinvoke $r1.getBoolean("startedFromLauncher", 0);

        if $z0 == 0 goto label1;

        return;

     label1:
        $r2 = virtualinvoke $r0.getIntent();

        if $r2 != null goto label2;

        return;

     label2:
        $r3 = "android.intent.action.MAIN";

        $r4 = virtualinvoke $r2.getAction();

        $z0 = virtualinvoke $r3.equals($r4);

        if $z0 == 0 goto label4;

        $z0 = virtualinvoke $r2.hasCategory("android.intent.category.LAUNCHER");

        if $z0 != 0 goto label3;

        $z0 = virtualinvoke $r2.hasCategory("android.intent.category.LEANBACK_LAUNCHER");

        if $z0 == 0 goto label4;

     label3:
        $r5 = interfaceinvoke $r1.edit();

        $r5 = interfaceinvoke $r5.putBoolean("startedFromLauncher", 1);

        interfaceinvoke $r5.apply();

     label4:
        return;
    }
}
