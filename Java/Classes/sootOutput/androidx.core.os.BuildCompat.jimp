public class androidx.core.os.BuildCompat extends java.lang.Object
{

    private void <init>()
    {
        androidx.core.os.BuildCompat r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static boolean isAtLeastN()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 24 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastNMR1()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 25 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastO()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 26 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastOMR1()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 27 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastP()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 28 goto label1;

        return 1;

     label1:
        return 0;
    }

    protected static boolean isAtLeastPreReleaseCodename(java.lang.String, java.lang.String)
    {
        int $i0;
        java.lang.String $r0, $r1, $r2;
        boolean $z0;

        $r0 := @parameter0;

        $r1 := @parameter1;

        $r2 = "REL";

        $z0 = virtualinvoke $r2.equals($r1);

        if $z0 == 0 goto label1;

        return 0;

     label1:
        $i0 = virtualinvoke $r1.compareTo($r0);

        if $i0 < 0 goto label2;

        return 1;

     label2:
        return 0;
    }

    public static boolean isAtLeastQ()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 29 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastR()
    {
        int $i0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 30 goto label1;

        return 1;

     label1:
        return 0;
    }

    public static boolean isAtLeastS()
    {
        int $i0;
        java.lang.String $r0;
        boolean $z0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 >= 31 goto label2;

        $r0 = android.os.Build$VERSION.CODENAME;

        $z0 = androidx.core.os.BuildCompat.isAtLeastPreReleaseCodename("S", $r0);

        if $z0 == 0 goto label1;

        goto label2;

     label1:
        return 0;

     label2:
        return 1;
    }

    public static boolean isAtLeastT()
    {
        java.lang.String $r0;
        boolean $z0;

        $r0 = android.os.Build$VERSION.CODENAME;

        $z0 = androidx.core.os.BuildCompat.isAtLeastPreReleaseCodename("T", $r0);

        return $z0;
    }
}
