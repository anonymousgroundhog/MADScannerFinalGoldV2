class androidx.appcompat.graphics.drawable.AnimatedStateListDrawableCompat$FrameInterpolator extends java.lang.Object implements android.animation.TimeInterpolator
{
    private int[] mFrameTimes;
    private int mFrames;
    private int mTotalDuration;

    void <init>(android.graphics.drawable.AnimationDrawable, boolean)
    {
        androidx.appcompat.graphics.drawable.AnimatedStateListDrawableCompat$FrameInterpolator r0;
        android.graphics.drawable.AnimationDrawable $r1;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

        $z0 := @parameter1;

        specialinvoke r0.<init>();

        virtualinvoke r0.updateFrames($r1, $z0);

        return;
    }

    public float getInterpolation(float)
    {
        androidx.appcompat.graphics.drawable.AnimatedStateListDrawableCompat$FrameInterpolator r0;
        int[] r1;
        int i0, i1, i2, i3;
        float $f0, $f1, $f2;

        r0 := @this;

        $f0 := @parameter0;

        i0 = r0.mTotalDuration;

        $f1 = (float) i0;

        $f0 = $f1 * $f0;

        $f0 = $f0 + 0.5F;

        i0 = r0.mFrames;

        r1 = r0.mFrameTimes;

        i2 = (int) $f0;

        i1 = 0;

     label1:
        if i1 >= i0 goto label2;

        i3 = r1[i1];

        if i2 < i3 goto label2;

        i3 = r1[i1];

        i2 = i2 - i3;

        i1 = i1 + 1;

        goto label1;

     label2:
        if i1 >= i0 goto label3;

        $f0 = (float) i2;

        i2 = r0.mTotalDuration;

        $f1 = (float) i2;

        $f0 = $f0 / $f1;

        goto label4;

     label3:
        $f0 = 0.0F;

     label4:
        $f2 = (float) i1;

        $f1 = (float) i0;

        $f1 = $f2 / $f1;

        $f0 = $f1 + $f0;

        return $f0;
    }

    int getTotalDuration()
    {
        androidx.appcompat.graphics.drawable.AnimatedStateListDrawableCompat$FrameInterpolator r0;
        int i0;

        r0 := @this;

        i0 = r0.mTotalDuration;

        return i0;
    }

    int updateFrames(android.graphics.drawable.AnimationDrawable, boolean)
    {
        android.graphics.drawable.AnimationDrawable $r1;
        androidx.appcompat.graphics.drawable.AnimatedStateListDrawableCompat$FrameInterpolator r0;
        int[] r2;
        int $i0, i1, i2, i3;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

        $z0 := @parameter1;

        $i0 = virtualinvoke $r1.getNumberOfFrames();

        r0.mFrames = $i0;

        r2 = r0.mFrameTimes;

        if r2 == null goto label1;

        i1 = lengthof r2;

        if i1 >= $i0 goto label2;

     label1:
        r2 = newarray (int)[$i0];

        r0.mFrameTimes = r2;

     label2:
        r2 = r0.mFrameTimes;

        i1 = 0;

        i2 = 0;

     label3:
        if i2 >= $i0 goto label6;

        if $z0 == 0 goto label4;

        i3 = $i0 - i2;

        i3 = i3 + -1;

        goto label5;

     label4:
        i3 = i2;

     label5:
        i3 = virtualinvoke $r1.getDuration(i3);

        r2[i2] = i3;

        i1 = i1 + i3;

        i2 = i2 + 1;

        goto label3;

     label6:
        r0.mTotalDuration = i1;

        return i1;
    }
}
