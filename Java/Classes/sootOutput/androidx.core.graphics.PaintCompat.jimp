public final class androidx.core.graphics.PaintCompat extends java.lang.Object
{
    private static final java.lang.String EM_STRING;
    private static final java.lang.String TOFU_STRING;
    private static final java.lang.ThreadLocal sRectThreadLocal;

    static void <clinit>()
    {
        java.lang.ThreadLocal $r0;

        $r0 = new java.lang.ThreadLocal;

        specialinvoke $r0.<init>();

        androidx.core.graphics.PaintCompat.sRectThreadLocal = $r0;

        return;
    }

    private void <init>()
    {
        androidx.core.graphics.PaintCompat r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static boolean hasGlyph(android.graphics.Paint, java.lang.String)
    {
        android.graphics.Rect r5;
        byte b2;
        int i0, $i3, $i4, $i5;
        java.lang.String $r1, $r2;
        boolean $z0;
        char $c1;
        androidx.core.util.Pair $r3;
        float $f0, $f1, $f2, $f3;
        android.graphics.Paint $r0;
        java.lang.Object r4;

        $r0 := @parameter0;

        $r1 := @parameter1;

        i0 = android.os.Build$VERSION.SDK_INT;

        if i0 < 23 goto label1;

        $z0 = virtualinvoke $r0.hasGlyph($r1);

        return $z0;

     label1:
        i0 = virtualinvoke $r1.length();

        if i0 != 1 goto label2;

        $c1 = virtualinvoke $r1.charAt(0);

        $z0 = java.lang.Character.isWhitespace($c1);

        if $z0 == 0 goto label2;

        return 1;

     label2:
        $r2 = "\udb3f\udffd";

        $f0 = virtualinvoke $r0.measureText("\udb3f\udffd");

        $f1 = virtualinvoke $r0.measureText("m");

        $f2 = virtualinvoke $r0.measureText($r1);

        b2 = $f2 cmpl 0.0F;

        if b2 != 0 goto label3;

        return 0;

     label3:
        $i3 = virtualinvoke $r1.length();

        $i3 = virtualinvoke $r1.codePointCount(0, $i3);

        if $i3 <= 1 goto label7;

        $f1 = 2.0F * $f1;

        b2 = $f2 cmpl $f1;

        $i3 = (int) b2;

        if $i3 <= 0 goto label4;

        return 0;

     label4:
        $f1 = 0.0F;

        $i3 = 0;

     label5:
        if $i3 >= i0 goto label6;

        $i4 = virtualinvoke $r1.codePointAt($i3);

        $i4 = java.lang.Character.charCount($i4);

        $i5 = $i3 + $i4;

        $f3 = virtualinvoke $r0.measureText($r1, $i3, $i5);

        $f1 = $f1 + $f3;

        $i3 = $i3 + $i4;

        goto label5;

     label6:
        b2 = $f2 cmpl $f1;

        $i3 = (int) b2;

        if $i3 < 0 goto label7;

        return 0;

     label7:
        b2 = $f2 cmpl $f0;

        if b2 == 0 goto label8;

        return 1;

     label8:
        $r3 = androidx.core.graphics.PaintCompat.obtainEmptyRects();

        $i3 = virtualinvoke $r2.length();

        r4 = $r3.first;

        r5 = (android.graphics.Rect) r4;

        virtualinvoke $r0.getTextBounds("\udb3f\udffd", 0, $i3, r5);

        r4 = $r3.second;

        r5 = (android.graphics.Rect) r4;

        virtualinvoke $r0.getTextBounds($r1, 0, i0, r5);

        r4 = $r3.first;

        r5 = (android.graphics.Rect) r4;

        r4 = $r3.second;

        $z0 = virtualinvoke r5.equals(r4);

        $z0 = 1 ^ $z0;

        return $z0;
    }

    private static androidx.core.util.Pair obtainEmptyRects()
    {
        android.graphics.Rect $r1, $r2;
        java.lang.ThreadLocal $r4;
        androidx.core.util.Pair $r0;
        java.lang.Object $r3;

        $r4 = androidx.core.graphics.PaintCompat.sRectThreadLocal;

        $r3 = virtualinvoke $r4.get();

        $r0 = (androidx.core.util.Pair) $r3;

        if $r0 != null goto label1;

        $r0 = new androidx.core.util.Pair;

        $r1 = new android.graphics.Rect;

        specialinvoke $r1.<init>();

        $r2 = new android.graphics.Rect;

        specialinvoke $r2.<init>();

        specialinvoke $r0.<init>($r1, $r2);

        virtualinvoke $r4.set($r0);

        return $r0;

     label1:
        $r3 = $r0.first;

        $r1 = (android.graphics.Rect) $r3;

        virtualinvoke $r1.setEmpty();

        $r3 = $r0.second;

        $r1 = (android.graphics.Rect) $r3;

        virtualinvoke $r1.setEmpty();

        return $r0;
    }

    public static boolean setBlendMode(android.graphics.Paint, androidx.core.graphics.BlendModeCompat)
    {
        androidx.core.graphics.BlendModeCompat $r1;
        android.graphics.PorterDuff$Mode $r4;
        android.graphics.BlendMode $r3;
        android.graphics.PorterDuffXfermode $r5;
        int i0;
        android.graphics.Paint $r0;
        java.lang.Object $r2;
        android.graphics.Xfermode $r6;

        $r0 := @parameter0;

        $r1 := @parameter1;

        i0 = android.os.Build$VERSION.SDK_INT;

        $r2 = null;

        if i0 < 29 goto label2;

        if $r1 == null goto label1;

        $r2 = androidx.core.graphics.BlendModeUtils.obtainBlendModeFromCompat($r1);

     label1:
        $r3 = (android.graphics.BlendMode) $r2;

        virtualinvoke $r0.setBlendMode($r3);

        return 1;

     label2:
        if $r1 == null goto label5;

        $r4 = androidx.core.graphics.BlendModeUtils.obtainPorterDuffFromCompat($r1);

        if $r4 == null goto label3;

        $r5 = new android.graphics.PorterDuffXfermode;

        $r2 = $r5;

        specialinvoke $r5.<init>($r4);

     label3:
        $r6 = (android.graphics.Xfermode) $r2;

        virtualinvoke $r0.setXfermode($r6);

        if $r4 == null goto label4;

        return 1;

     label4:
        return 0;

     label5:
        virtualinvoke $r0.setXfermode(null);

        return 1;
    }
}
