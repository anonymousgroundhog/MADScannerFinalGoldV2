class androidx.work.impl.WorkDatabase$2 extends androidx.room.RoomDatabase$Callback
{

    void <init>()
    {
        androidx.work.impl.WorkDatabase$2 r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public void onOpen(androidx.sqlite.db.SupportSQLiteDatabase)
    {
        java.lang.Throwable $r3;
        androidx.sqlite.db.SupportSQLiteDatabase $r1;
        androidx.work.impl.WorkDatabase$2 r0;
        java.lang.String $r2;

        r0 := @this;

        $r1 := @parameter0;

        specialinvoke r0.onOpen($r1);

        interfaceinvoke $r1.beginTransaction();

     label1:
        $r2 = androidx.work.impl.WorkDatabase.getPruneSQL();

        interfaceinvoke $r1.execSQL($r2);

        interfaceinvoke $r1.setTransactionSuccessful();

     label2:
        interfaceinvoke $r1.endTransaction();

        return;

     label3:
        $r3 := @caughtexception;

        interfaceinvoke $r1.endTransaction();

        throw $r3;

        catch java.lang.Throwable from label1 to label2 with label3;
    }
}
