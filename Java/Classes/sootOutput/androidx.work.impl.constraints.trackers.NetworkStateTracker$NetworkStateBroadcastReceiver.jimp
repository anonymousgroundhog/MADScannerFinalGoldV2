class androidx.work.impl.constraints.trackers.NetworkStateTracker$NetworkStateBroadcastReceiver extends android.content.BroadcastReceiver
{
    final androidx.work.impl.constraints.trackers.NetworkStateTracker this$0;

    void <init>(androidx.work.impl.constraints.trackers.NetworkStateTracker)
    {
        androidx.work.impl.constraints.trackers.NetworkStateTracker $r1;
        androidx.work.impl.constraints.trackers.NetworkStateTracker$NetworkStateBroadcastReceiver r0;

        r0 := @this;

        $r1 := @parameter0;

        r0.this$0 = $r1;

        specialinvoke r0.<init>();

        return;
    }

    public void onReceive(android.content.Context, android.content.Intent)
    {
        android.content.Context $r1;
        androidx.work.impl.constraints.trackers.NetworkStateTracker $r6;
        android.content.Intent $r2;
        androidx.work.Logger $r4;
        androidx.work.impl.constraints.NetworkState $r7;
        java.lang.Throwable[] $r5;
        androidx.work.impl.constraints.trackers.NetworkStateTracker$NetworkStateBroadcastReceiver r0;
        java.lang.String $r3;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

        $r2 := @parameter1;

        if $r2 == null goto label2;

        $r3 = virtualinvoke $r2.getAction();

        if $r3 != null goto label1;

        return;

     label1:
        $r3 = virtualinvoke $r2.getAction();

        $z0 = virtualinvoke $r3.equals("android.net.conn.CONNECTIVITY_CHANGE");

        if $z0 == 0 goto label2;

        $r4 = androidx.work.Logger.get();

        $r3 = androidx.work.impl.constraints.trackers.NetworkStateTracker.TAG;

        $r5 = newarray (java.lang.Throwable)[0];

        virtualinvoke $r4.debug($r3, "Network broadcast received", $r5);

        $r6 = r0.this$0;

        $r7 = virtualinvoke $r6.getActiveNetworkState();

        virtualinvoke $r6.setState($r7);

     label2:
        return;
    }
}
