public final enum class androidx.room.RoomDatabase$JournalMode extends java.lang.Enum
{
    private static final androidx.room.RoomDatabase$JournalMode[] $VALUES;
    public static final enum androidx.room.RoomDatabase$JournalMode AUTOMATIC;
    public static final enum androidx.room.RoomDatabase$JournalMode TRUNCATE;
    public static final enum androidx.room.RoomDatabase$JournalMode WRITE_AHEAD_LOGGING;

    static void <clinit>()
    {
        androidx.room.RoomDatabase$JournalMode $r0, $r1, $r2;
        androidx.room.RoomDatabase$JournalMode[] $r3;

        $r0 = new androidx.room.RoomDatabase$JournalMode;

        specialinvoke $r0.<init>("AUTOMATIC", 0);

        androidx.room.RoomDatabase$JournalMode.AUTOMATIC = $r0;

        $r1 = new androidx.room.RoomDatabase$JournalMode;

        specialinvoke $r1.<init>("TRUNCATE", 1);

        androidx.room.RoomDatabase$JournalMode.TRUNCATE = $r1;

        $r2 = new androidx.room.RoomDatabase$JournalMode;

        specialinvoke $r2.<init>("WRITE_AHEAD_LOGGING", 2);

        androidx.room.RoomDatabase$JournalMode.WRITE_AHEAD_LOGGING = $r2;

        $r3 = newarray (androidx.room.RoomDatabase$JournalMode)[3];

        $r3[0] = $r0;

        $r3[1] = $r1;

        $r3[2] = $r2;

        androidx.room.RoomDatabase$JournalMode.$VALUES = $r3;

        return;
    }

    private void <init>(java.lang.String, int)
    {
        int $i0;
        androidx.room.RoomDatabase$JournalMode r0;
        java.lang.String $r1;

        r0 := @this;

        $r1 := @parameter0;

        $i0 := @parameter1;

        specialinvoke r0.<init>($r1, $i0);

        return;
    }

    private static boolean isLowRamDevice(android.app.ActivityManager)
    {
        android.app.ActivityManager $r0;
        boolean $z0;

        $r0 := @parameter0;

        $z0 = virtualinvoke $r0.isLowRamDevice();

        return $z0;
    }

    public static androidx.room.RoomDatabase$JournalMode valueOf(java.lang.String)
    {
        androidx.room.RoomDatabase$JournalMode $r2;
        java.lang.String $r0;
        java.lang.Enum $r1;

        $r0 := @parameter0;

        $r1 = java.lang.Enum.valueOf(class "Landroidx/room/RoomDatabase$JournalMode;", $r0);

        $r2 = (androidx.room.RoomDatabase$JournalMode) $r1;

        return $r2;
    }

    public static androidx.room.RoomDatabase$JournalMode[] values()
    {
        java.lang.Object $r0;
        androidx.room.RoomDatabase$JournalMode[] $r1;

        $r1 = androidx.room.RoomDatabase$JournalMode.$VALUES;

        $r0 = virtualinvoke $r1.clone();

        $r1 = (androidx.room.RoomDatabase$JournalMode[]) $r0;

        return $r1;
    }

    androidx.room.RoomDatabase$JournalMode resolve(android.content.Context)
    {
        android.content.Context $r1;
        android.app.ActivityManager r4;
        androidx.room.RoomDatabase$JournalMode r0, r2;
        java.lang.Object $r3;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

        r2 = androidx.room.RoomDatabase$JournalMode.AUTOMATIC;

        if r0 == r2 goto label1;

        return r0;

     label1:
        $r3 = virtualinvoke $r1.getSystemService("activity");

        r4 = (android.app.ActivityManager) $r3;

        if r4 == null goto label2;

        $z0 = androidx.room.RoomDatabase$JournalMode.isLowRamDevice(r4);

        if $z0 != 0 goto label2;

        r0 = androidx.room.RoomDatabase$JournalMode.WRITE_AHEAD_LOGGING;

        return r0;

     label2:
        r0 = androidx.room.RoomDatabase$JournalMode.TRUNCATE;

        return r0;
    }
}
