class androidx.appcompat.app.TwilightManager extends java.lang.Object
{
    private static final int SUNRISE;
    private static final int SUNSET;
    private static final java.lang.String TAG;
    private static androidx.appcompat.app.TwilightManager sInstance;
    private final android.content.Context mContext;
    private final android.location.LocationManager mLocationManager;
    private final androidx.appcompat.app.TwilightManager$TwilightState mTwilightState;

    void <init>(android.content.Context, android.location.LocationManager)
    {
        android.content.Context $r1;
        androidx.appcompat.app.TwilightManager$TwilightState $r3;
        androidx.appcompat.app.TwilightManager r0;
        android.location.LocationManager $r2;

        r0 := @this;

        $r1 := @parameter0;

        $r2 := @parameter1;

        specialinvoke r0.<init>();

        $r3 = new androidx.appcompat.app.TwilightManager$TwilightState;

        specialinvoke $r3.<init>();

        r0.mTwilightState = $r3;

        r0.mContext = $r1;

        r0.mLocationManager = $r2;

        return;
    }

    static androidx.appcompat.app.TwilightManager getInstance(android.content.Context)
    {
        android.content.Context $r0;
        java.lang.Object $r2;
        androidx.appcompat.app.TwilightManager $r1;
        android.location.LocationManager $r3;

        $r0 := @parameter0;

        $r1 = androidx.appcompat.app.TwilightManager.sInstance;

        if $r1 != null goto label1;

        $r0 = virtualinvoke $r0.getApplicationContext();

        $r1 = new androidx.appcompat.app.TwilightManager;

        $r2 = virtualinvoke $r0.getSystemService("location");

        $r3 = (android.location.LocationManager) $r2;

        specialinvoke $r1.<init>($r0, $r3);

        androidx.appcompat.app.TwilightManager.sInstance = $r1;

     label1:
        $r1 = androidx.appcompat.app.TwilightManager.sInstance;

        return $r1;
    }

    private android.location.Location getLastKnownLocation()
    {
        android.content.Context r3;
        long $l2, $l3;
        byte $b0;
        int $i1;
        androidx.appcompat.app.TwilightManager r0;
        android.location.Location r1, r2;

        r0 := @this;

        r1 = null;

        r2 = null;

        r3 = r0.mContext;

        $i1 = androidx.core.content.PermissionChecker.checkSelfPermission(r3, "android.permission.ACCESS_COARSE_LOCATION");

        if $i1 != 0 goto label1;

        r1 = specialinvoke r0.getLastKnownLocationForProvider("network");

     label1:
        r3 = r0.mContext;

        $i1 = androidx.core.content.PermissionChecker.checkSelfPermission(r3, "android.permission.ACCESS_FINE_LOCATION");

        if $i1 != 0 goto label2;

        r2 = specialinvoke r0.getLastKnownLocationForProvider("gps");

     label2:
        if r2 == null goto label4;

        if r1 == null goto label4;

        $l2 = virtualinvoke r2.getTime();

        $l3 = virtualinvoke r1.getTime();

        $b0 = $l2 cmp $l3;

        $i1 = (int) $b0;

        if $i1 <= 0 goto label3;

        return r2;

     label3:
        return r1;

     label4:
        if r2 == null goto label5;

        return r2;

     label5:
        return r1;
    }

    private android.location.Location getLastKnownLocationForProvider(java.lang.String)
    {
        androidx.appcompat.app.TwilightManager r0;
        java.lang.String $r1;
        android.location.Location $r3;
        android.location.LocationManager r2;
        java.lang.Exception r4;
        boolean $z0;

        r0 := @this;

        $r1 := @parameter0;

     label1:
        r2 = r0.mLocationManager;

        $z0 = virtualinvoke r2.isProviderEnabled($r1);

     label2:
        if $z0 == 0 goto label5;

     label3:
        r2 = r0.mLocationManager;

        $r3 = virtualinvoke r2.getLastKnownLocation($r1);

     label4:
        return $r3;

     label5:
        goto label7;

     label6:
        r4 := @caughtexception;

        android.util.Log.d("TwilightManager", "Failed to get last known location", r4);

     label7:
        return null;

        catch java.lang.Exception from label1 to label2 with label6;
        catch java.lang.Exception from label3 to label4 with label6;
    }

    private boolean isStateValid()
    {
        androidx.appcompat.app.TwilightManager$TwilightState $r1;
        byte $b0;
        int $i3;
        long $l1, $l2;
        androidx.appcompat.app.TwilightManager r0;

        r0 := @this;

        $r1 = r0.mTwilightState;

        $l2 = $r1.nextUpdate;

        $l1 = java.lang.System.currentTimeMillis();

        $b0 = $l2 cmp $l1;

        $i3 = (int) $b0;

        if $i3 <= 0 goto label1;

        return 1;

     label1:
        return 0;
    }

    static void setInstance(androidx.appcompat.app.TwilightManager)
    {
        androidx.appcompat.app.TwilightManager $r0;

        $r0 := @parameter0;

        androidx.appcompat.app.TwilightManager.sInstance = $r0;

        return;
    }

    private void updateState(android.location.Location)
    {
        androidx.appcompat.app.TwilightManager$TwilightState r2;
        long $l0, l1, l3, l4, l5;
        androidx.appcompat.app.TwilightCalculator $r3;
        byte $b6;
        int i2;
        androidx.appcompat.app.TwilightManager r0;
        android.location.Location $r1;
        double $d0, $d1;
        boolean z0;

        r0 := @this;

        $r1 := @parameter0;

        r2 = r0.mTwilightState;

        $l0 = java.lang.System.currentTimeMillis();

        $r3 = androidx.appcompat.app.TwilightCalculator.getInstance();

        l1 = $l0 - 86400000L;

        $d0 = virtualinvoke $r1.getLatitude();

        $d1 = virtualinvoke $r1.getLongitude();

        virtualinvoke $r3.calculateTwilight(l1, $d0, $d1);

        l1 = $r3.sunset;

        $d0 = virtualinvoke $r1.getLatitude();

        $d1 = virtualinvoke $r1.getLongitude();

        virtualinvoke $r3.calculateTwilight($l0, $d0, $d1);

        i2 = $r3.state;

        z0 = 1;

        if i2 != 1 goto label1;

        goto label2;

     label1:
        z0 = 0;

     label2:
        l3 = $r3.sunrise;

        l4 = $r3.sunset;

        l5 = 86400000L + $l0;

        $d0 = virtualinvoke $r1.getLatitude();

        $d1 = virtualinvoke $r1.getLongitude();

        virtualinvoke $r3.calculateTwilight(l5, $d0, $d1);

        l5 = $r3.sunrise;

        $b6 = l3 cmp -1L;

        if $b6 == 0 goto label7;

        $b6 = l4 cmp -1L;

        if $b6 != 0 goto label3;

        goto label7;

     label3:
        $b6 = $l0 cmp l4;

        i2 = (int) $b6;

        if i2 <= 0 goto label4;

        $l0 = 0L + l5;

        goto label6;

     label4:
        $b6 = $l0 cmp l3;

        i2 = (int) $b6;

        if i2 <= 0 goto label5;

        $l0 = 0L + l4;

        goto label6;

     label5:
        $l0 = 0L + l3;

     label6:
        $l0 = $l0 + 60000L;

        goto label8;

     label7:
        $l0 = $l0 + 43200000L;

     label8:
        r2.isNight = z0;

        r2.yesterdaySunset = l1;

        r2.todaySunrise = l3;

        r2.todaySunset = l4;

        r2.tomorrowSunrise = l5;

        r2.nextUpdate = $l0;

        return;
    }

    boolean isNight()
    {
        androidx.appcompat.app.TwilightManager$TwilightState r1;
        java.util.Calendar $r3;
        int $i0;
        androidx.appcompat.app.TwilightManager r0;
        android.location.Location $r2;
        boolean $z0;

        r0 := @this;

        r1 = r0.mTwilightState;

        $z0 = specialinvoke r0.isStateValid();

        if $z0 == 0 goto label1;

        $z0 = r1.isNight;

        return $z0;

     label1:
        $r2 = specialinvoke r0.getLastKnownLocation();

        if $r2 == null goto label2;

        specialinvoke r0.updateState($r2);

        $z0 = r1.isNight;

        return $z0;

     label2:
        android.util.Log.i("TwilightManager", "Could not get last known location. This is probably because the app does not have any location permissions. Falling back to hardcoded sunrise/sunset values.");

        $r3 = java.util.Calendar.getInstance();

        $i0 = virtualinvoke $r3.get(11);

        if $i0 < 6 goto label4;

        if $i0 < 22 goto label3;

        goto label4;

     label3:
        return 0;

     label4:
        return 1;
    }
}
