public final class androidx.core.os.MessageCompat extends java.lang.Object
{
    private static boolean sTryIsAsynchronous;
    private static boolean sTrySetAsynchronous;

    static void <clinit>()
    {
        androidx.core.os.MessageCompat.sTrySetAsynchronous = 1;

        androidx.core.os.MessageCompat.sTryIsAsynchronous = 1;

        return;
    }

    private void <init>()
    {
        androidx.core.os.MessageCompat r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static boolean isAsynchronous(android.os.Message)
    {
        android.os.Message $r0;
        java.lang.NoSuchMethodError r1;
        int i0;
        boolean $z0;

        $r0 := @parameter0;

        i0 = android.os.Build$VERSION.SDK_INT;

        if i0 < 22 goto label1;

        $z0 = virtualinvoke $r0.isAsynchronous();

        return $z0;

     label1:
        $z0 = androidx.core.os.MessageCompat.sTryIsAsynchronous;

        if $z0 == 0 goto label5;

     label2:
        $z0 = virtualinvoke $r0.isAsynchronous();

     label3:
        return $z0;

     label4:
        r1 := @caughtexception;

        androidx.core.os.MessageCompat.sTryIsAsynchronous = 0;

     label5:
        return 0;

        catch java.lang.NoSuchMethodError from label2 to label3 with label4;
    }

    public static void setAsynchronous(android.os.Message, boolean)
    {
        android.os.Message $r0;
        java.lang.NoSuchMethodError r1;
        int i0;
        boolean $z0, z1;

        $r0 := @parameter0;

        $z0 := @parameter1;

        i0 = android.os.Build$VERSION.SDK_INT;

        if i0 < 22 goto label1;

        virtualinvoke $r0.setAsynchronous($z0);

        return;

     label1:
        z1 = androidx.core.os.MessageCompat.sTrySetAsynchronous;

        if z1 == 0 goto label5;

     label2:
        virtualinvoke $r0.setAsynchronous($z0);

     label3:
        return;

     label4:
        r1 := @caughtexception;

        androidx.core.os.MessageCompat.sTrySetAsynchronous = 0;

     label5:
        return;

        catch java.lang.NoSuchMethodError from label2 to label3 with label4;
    }
}
