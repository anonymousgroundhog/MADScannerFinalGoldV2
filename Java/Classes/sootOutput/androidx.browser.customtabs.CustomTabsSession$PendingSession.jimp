public class androidx.browser.customtabs.CustomTabsSession$PendingSession extends java.lang.Object
{
    private final androidx.browser.customtabs.CustomTabsCallback mCallback;
    private final android.app.PendingIntent mId;

    void <init>(androidx.browser.customtabs.CustomTabsCallback, android.app.PendingIntent)
    {
        androidx.browser.customtabs.CustomTabsSession$PendingSession r0;
        androidx.browser.customtabs.CustomTabsCallback $r1;
        android.app.PendingIntent $r2;

        r0 := @this;

        $r1 := @parameter0;

        $r2 := @parameter1;

        specialinvoke r0.<init>();

        r0.mCallback = $r1;

        r0.mId = $r2;

        return;
    }

    androidx.browser.customtabs.CustomTabsCallback getCallback()
    {
        androidx.browser.customtabs.CustomTabsSession$PendingSession r0;
        androidx.browser.customtabs.CustomTabsCallback r1;

        r0 := @this;

        r1 = r0.mCallback;

        return r1;
    }

    android.app.PendingIntent getId()
    {
        androidx.browser.customtabs.CustomTabsSession$PendingSession r0;
        android.app.PendingIntent r1;

        r0 := @this;

        r1 = r0.mId;

        return r1;
    }
}
