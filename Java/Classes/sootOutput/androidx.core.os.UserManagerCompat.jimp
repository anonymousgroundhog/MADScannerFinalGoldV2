public class androidx.core.os.UserManagerCompat extends java.lang.Object
{

    private void <init>()
    {
        androidx.core.os.UserManagerCompat r0;

        r0 := @this;

        specialinvoke r0.<init>();

        return;
    }

    public static boolean isUserUnlocked(android.content.Context)
    {
        android.os.UserManager $r2;
        android.content.Context $r0;
        int $i0;
        java.lang.Object $r1;
        boolean $z0;

        $r0 := @parameter0;

        $i0 = android.os.Build$VERSION.SDK_INT;

        if $i0 < 24 goto label1;

        $r1 = virtualinvoke $r0.getSystemService(class "Landroid/os/UserManager;");

        $r2 = (android.os.UserManager) $r1;

        $z0 = virtualinvoke $r2.isUserUnlocked();

        return $z0;

     label1:
        return 1;
    }
}
