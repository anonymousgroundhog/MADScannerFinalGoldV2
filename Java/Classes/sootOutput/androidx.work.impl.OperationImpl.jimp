public class androidx.work.impl.OperationImpl extends java.lang.Object implements androidx.work.Operation
{
    private final androidx.work.impl.utils.futures.SettableFuture mOperationFuture;
    private final androidx.lifecycle.MutableLiveData mOperationState;

    public void <init>()
    {
        androidx.lifecycle.MutableLiveData $r2;
        androidx.work.impl.utils.futures.SettableFuture $r1;
        androidx.work.Operation$State$IN_PROGRESS $r3;
        androidx.work.impl.OperationImpl r0;

        r0 := @this;

        specialinvoke r0.<init>();

        $r2 = new androidx.lifecycle.MutableLiveData;

        specialinvoke $r2.<init>();

        r0.mOperationState = $r2;

        $r1 = androidx.work.impl.utils.futures.SettableFuture.create();

        r0.mOperationFuture = $r1;

        $r3 = androidx.work.Operation.IN_PROGRESS;

        virtualinvoke r0.setState($r3);

        return;
    }

    public com.google.common.util.concurrent.ListenableFuture getResult()
    {
        androidx.work.impl.utils.futures.SettableFuture r1;
        androidx.work.impl.OperationImpl r0;

        r0 := @this;

        r1 = r0.mOperationFuture;

        return r1;
    }

    public androidx.lifecycle.LiveData getState()
    {
        androidx.lifecycle.MutableLiveData r1;
        androidx.work.impl.OperationImpl r0;

        r0 := @this;

        r1 = r0.mOperationState;

        return r1;
    }

    public void setState(androidx.work.Operation$State)
    {
        java.lang.Throwable $r2;
        androidx.lifecycle.MutableLiveData r3;
        androidx.work.Operation$State$SUCCESS $r5;
        androidx.work.Operation$State$FAILURE r6;
        androidx.work.impl.utils.futures.SettableFuture r4;
        androidx.work.impl.OperationImpl r0;
        androidx.work.Operation$State $r1;
        boolean z0;

        r0 := @this;

        $r1 := @parameter0;

        r3 = r0.mOperationState;

        virtualinvoke r3.postValue($r1);

        z0 = $r1 instanceof androidx.work.Operation$State$SUCCESS;

        if z0 == 0 goto label1;

        r4 = r0.mOperationFuture;

        $r5 = (androidx.work.Operation$State$SUCCESS) $r1;

        virtualinvoke r4.set($r5);

        return;

     label1:
        z0 = $r1 instanceof androidx.work.Operation$State$FAILURE;

        if z0 == 0 goto label2;

        r6 = (androidx.work.Operation$State$FAILURE) $r1;

        r4 = r0.mOperationFuture;

        $r2 = virtualinvoke r6.getThrowable();

        virtualinvoke r4.setException($r2);

     label2:
        return;
    }
}
